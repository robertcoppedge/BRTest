package brtest.assets;

import java.util.Date;

public class image extends asset {

    public image (int ID, String name, String type, String url, String expires)
    {
        super(ID, name, type, url, expires);
    }

}
